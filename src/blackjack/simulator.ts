import { Action } from "./blackjack";

export default function simulate(
  count: number,
  playerCards: number[],
  dealerCard: number
) {
  // create deck

  // deal out player cards, dealer card

  // square deck with count

  const actions = [Action.Stay, Action.Hit];

  // execute each action

  // get action with the best outcome
}
